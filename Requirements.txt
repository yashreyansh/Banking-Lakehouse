# synthetic_data/requirements.txt
# Install with: pip install -r requirements.txt
 
# ── Core data generation ──────────────────────────
faker==24.9.0               # realistic fake data (es_MX locale)
pandas==2.2.2               # DataFrame manipulation
numpy==1.26.4               # numeric distributions (lognormal, etc.)
pyarrow==16.0.0             # Parquet read/write
pyyaml==6.0.1               # config.yml parsing
 
# ── Azure Event Hub publishing ────────────────────
azure-eventhub==5.11.7      # Event Hub producer client
azure-identity==1.16.0      # AAD / managed identity auth (optional)
 
# ── Optional utilities ────────────────────────────
python-dotenv==1.0.1        # load .env for EVENTHUB_CONNECTION_STRING
tqdm==4.66.4                # progress bars (optional)
sqlalchemy==2.0.49
pyodbc==5.3.0
dotenv==0.9.9